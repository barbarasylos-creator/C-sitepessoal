# SDD ledger — plan: docs/superpowers/plans/2026-09-23-site-pessoal-implementation.md

Ruling: projeto não é um repositório git e a usuária decidiu não usar git — script `sdd-workspace`/worktree (dependem de `git rev-parse --show-toplevel`) não usados; execução direta na pasta do projeto, ledger mantido como arquivo simples em vez do workspace `.superpowers/sdd/`. Custo se errado: nenhum, é só uma escolha de tooling, não afeta o código produzido.

Ruling: a skill genérica test-driven-development pressupõe framework de teste automatizado (`npm test` etc.); a spec do projeto (aprovada pela usuária) exige explicitamente "sem testes automatizados, sem build, sem npm". Adotado o próprio contrato de verificação manual do plano (passos "Verificação manual" com "Esperado:") como o gate RED/GREEN equivalente para cada tarefa, em vez de introduzir um test runner. Custo se errado: nenhum — introduzir um framework de teste violaria uma restrição já aprovada na spec.

Pre-flight: 2 linhas de interface compartilhada revisadas — Task 1 produz `#menu-toggle`, `#nav-links`/`.nav-links`/`.nav-link`, `#theme-toggle`, âncoras `#sobre #projetos #contato`, `.cta-button`, `.cards`/`.card`/`.card-link`; Task 2 consome esses mesmos seletores e produz `[data-theme="dark"]` e `.nav-links.is-open`; Task 3 consome os seletores de Task 1 e as convenções de Task 2. Nomes conferidos byte a byte entre as três tarefas do plano — sem divergência encontrada.

Ruling: a extensão Claude in Chrome não foi instalada nesta sessão (usuária optou por não instalar), então não há ferramenta de automação de navegador disponível para mim. Substituí os passos de "verificação manual no navegador" do plano por verificação estática do arquivo (grep confirmando ausência de caminhos absolutos `href="/`/`src="/` e presença de todos os ids/seletores esperados) e abri cada versão no navegador padrão do Windows (`Start-Process`) para a usuária conferir visualmente por conta própria. Custo se errado: um problema puramente visual/interativo (ex.: um clique que não funciona) passaria despercebido por mim até a usuária testar; peço confirmação dela ao final.

Task 1: complete (arquivos: index.html criado com estrutura/conteúdo completos; style.css e script.js criados vazios; verificação estática: sem caminhos absolutos, todos os ids do contrato presentes — sobre, projetos, contato, menu-toggle, nav-links, theme-toggle; aberto no navegador padrão via Start-Process para conferência visual da usuária)

Task 2: complete (style.css completo escrito conforme o plano; verificação estática: chaves balanceadas 25/25, seletores de contrato presentes — `[data-theme="dark"]`, `.nav-links.is-open`, `@media (max-width: 768px)`; verificação visual/interativa nos passos 2-5 do plano — desktop, layout mobile via `is-open` manual, tema escuro via atributo manual, resize mobile↔desktop — não executada por mim por falta de ferramenta de navegador nesta sessão, fica pendente de conferência da usuária junto com o Task 1)

Task 3: complete (script.js completo escrito conforme o plano; verificação: `node --check script.js` → sintaxe OK; grep confirma que todos os seletores usados — #theme-toggle, #menu-toggle, #nav-links, data-theme, is-open — batem exatamente com os produzidos nas Tasks 1 e 2; index.html reaberto no navegador padrão com os três arquivos completos. Verificação interativa dos passos 2-7 do plano — toggle de tema, persistência via F5, prioridade sobre prefers-color-scheme, menu mobile, fechar menu ao clicar em link, cópia para subpasta — não executada por mim por falta de ferramenta de navegador nesta sessão; fica pendente de conferência da usuária)

Final review: subagente (general-purpose, modelo opus) — leu spec, plano, ledger e os três arquivos completos (sem diff de git, projeto sem repositório). Achou 2 Críticos, 4 Importantes, 10 Menores.

Final: fixed exceção de localStorage derrubava o script inteiro (tema + hambúrguer) em contextos sem acesso a storage (file://, modo privativo) — try/catch em `getPreferredTheme` e `toggleTheme` (script.js), verificado com `node --check` (sintaxe) e leitura do fluxo: falha de storage agora só perde a persistência, sem quebrar o resto.
Final: fixed links de contato ilegíveis no tema escuro (herdavam azul/roxo padrão do navegador, ~2:1 de contraste) — regra `#contato a { color: var(--color-primary); text-decoration: underline; }` em style.css.
Final: fixed cartões de projeto nunca ficavam 3 por linha (`minmax(15rem,1fr)` exigia 48rem, a seção só tem 45rem de conteúdo) — trocado para `minmax(13rem, 1fr)` (3×13+2×1,5=42rem, cabe).
Final: fixed botão "Fale comigo" com texto branco sobre azul claro no tema escuro (~2,5:1) — `[data-theme="dark"] .cta-button { color: #121212; }` (7,4:1).
Final: fixed flash de tema claro ao recarregar com tema escuro salvo (script.js só carrega no fim do body) — script inline no `<head>` do index.html aplica `data-theme` a partir do localStorage (com try/catch) antes da primeira pintura; script.js continua cuidando do ícone/botão.
Final: fixed `href="#"` nos três cartões placeholder rolava a página pro topo suavemente e empilhava entrada no histórico — trocado `<a href="#" class="card-link">` por `<span class="card-link">` nos três cartões, com comentário lembrando de trocar por link real depois.
Verificação dos 6 fixes: `node --check script.js` → OK; chaves do CSS balanceadas (27/27); grep confirma zero `href="/`/`src="/` e zero `href="#"` remanescente; index.html reaberto no navegador padrão. Confirmação visual/interativa completa (inclusive contraste real na tela e grid em 3 colunas) segue pendente da usuária, mesmo motivo dos Tasks 1-3 (sem ferramenta de navegador nesta sessão).

Final: minor (deferred): aria-label do hambúrguer fica "Abrir menu" mesmo com o menu já aberto (index.html) — trocar para "Fechar menu" dinamicamente melhoraria leitor de tela.
Final: minor (deferred): id="nav" em index.html não é usado por nenhum seletor (CSS usa .nav) — atributo morto.
Final: minor (deferred): botões #menu-toggle e #theme-toggle sem type="button" — sem forms na página não causa bug hoje.
Final: minor (deferred): falta `color-scheme: light dark` / `dark` no CSS — barra de rolagem e controles nativos não acompanham o tema escuro.
Final: minor (deferred): `scroll-behavior: smooth` não respeita `prefers-reduced-motion: reduce`.
Final: minor (deferred): `transition` só no body — bordas de .header/.card/.theme-toggle trocam de cor sem fade, troca de tema fica irregular.
Final: minor (deferred): `.card-link` sem :hover, inconsistente com `.nav-link` que tem.
Final: minor (deferred): lógica de fechar o menu duplicada entre toggleMobileMenu e o listener dos nav-links (script.js) — extrair closeMobileMenu() evitaria diver­gência futura.
Final: minor (deferred): menu mobile aberto sobrevive ao resize para desktop e volta ao mobile ainda aberto — não é estado quebrado, mas pode surpreender.
Final: minor (deferred): sem fechar o menu mobile com Esc ou clique fora.

Final: Ruling: revisor descartou do julgamento (conteúdo placeholder dos projetos, formulário/analytics/SEO/robots/sitemap, testes automatizados/build/lint, favicon, footer com copyright, âncoras em português, `rel="noopener"` sem `noreferrer`, listener de mudança de tema do sistema em tempo real, focus trap no menu, header sticky/scroll-spy/animações extras, namespacing da chave do localStorage, ambiguidade do breakpoint exato em 768px, estilo de impressão) — mantidos todos fora de escopo, concordo com o revisor: cada um está coberto pela seção "Fora de escopo" da spec ou por uma restrição explícita já registrada (sem testes/build, decisão de não usar git). Custo se errado: baixo — são todos itens de polimento ou funcionalidade não pedida, fáceis de adicionar depois se a usuária quiser.
Final: Ruling: revisor apontou a pasta `docs/` na raiz do projeto contra a regra da spec de "exatamente três arquivos na raiz" sem julgar. Decisão: manter — a regra da spec descreve os arquivos do site entregue (index.html/style.css/script.js, sem README/config), e `docs/` guarda artefatos do processo de planejamento (spec, plano, este ledger), não código nem configuração do site. Custo se errado: baixo — a usuária pode apagar `docs/` a qualquer momento sem afetar o funcionamento do site.

Final review: sem repositório git neste projeto, não há branch para "superpowers:finishing-a-development-branch" nem workspace `.superpowers/sdd/` a apagar — este arquivo de ledger fica salvo em `docs/superpowers/plans/` como o registro permanente das decisões, já que não há histórico de git para cumprir esse papel.
